# XAUUSD TikTok Profit Generator (Viral)

## What’s new
- “ENTRY → TP HIT” pop animation
- Session toggle: London / NY
- Always-on watermark in the video: `@mcm_trading`
- Landing shown in video + on page: `mcmtrading.netlify.app`

## Deploy on Render (fastest)
1) Upload these files to a GitHub repo
2) Render -> New + -> Blueprint -> connect repo -> Deploy
(Render reads `render.yaml` automatically.)

## Endpoints
- GET `/` shows the form
- POST `/generate` returns a downloadable MP4

## Notes
- Video generation can take a few seconds.
